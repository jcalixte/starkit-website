# Reference

The four pages the Guide keeps pointing at. The compile-checked truth is
[`src/starkit.gleam`](https://github.com/jcalixte/starkit/blob/main/seed/src/starkit.gleam); where
this site and that module disagree, the module wins.

- [The Vocabulary](/reference/vocabulary) — every type and constructor a Script is allowed to name.
- [What you can import](/reference/imports) — the packages already vendored into `~/.starkit`.
- [Limits](/reference/limits) — timeouts, sizes, and the ceilings the Shelf enforces.
- [Which files are yours](/reference/files) — what you may edit under `~/.starkit`, and what the
  Watcher rewrites.
